# Copy this file to .env.local
# PowerShell:
#   Copy-Item env.template .env.local -Force

# PostgreSQL connection string (matches docker-compose)
DATABASE_URL=postgresql://parkovka:parkovka@localhost:5432/parkovka?schema=public

# JWT secret for session tokens (change in production)
JWT_SECRET=change-me-please

# Optional: use mocks instead of DB (leave empty for real DB)
# USE_MOCKS=1
